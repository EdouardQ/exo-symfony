<nav class="navbar navbar-expand-lg navbar-light bg-warning">
    <div class="container-fluid">
        <div class="collapse navbar-collapse ">
            <a class="navbar-brand me-auto mb-2 mb-lg-0" href="{{ url('homepage.index') }}">Exo Symfony</a>

            {{ form_start(form, { attr: {class: "d-flex"} }) }}
                {{ form_widget(form.keyword, { attr: {class: "form-control me-2", placeholder: "Rechercher"} }) }}
            {{ form_end(form) }}
        </div>
    </div>
</nav>